# Claude AI A · 공식 요청 1

이 문서를 읽으라는 사용자의 메시지부터 T05 인계 실험의 **공식 요청 1회**로 센다.
그 메시지를 받은 시각부터 30분 벽시계 타이머를 시작한다.

당신은 학원 PC에서 작업하는 **AI A · Claude**다. 다음 AI인 Codex는 이 대화를
받지 않으며, 나중에 저장소와 `docs/HANDOFF.md`만으로 작업을 이어간다.

## 이번 요청에서 할 일

아래 순서를 지키고, 이번 응답에서는 검사 1까지만 구현한다.

1. `docs/EXPERIMENT_PLAN.md`, `docs/START-CLAUDE.md`, `docs/AI-A-LOG.md`를 읽는다.
2. `git status`와 `git rev-parse HEAD`를 확인한다.
3. `docs/AI-A-LOG.md`에 다음 시작 정보를 즉시 기록한다.
   - 화면에 표시된 정확한 Claude 모델명
   - 현재 한국 시각
   - 시작 커밋 SHA
   - 요청 횟수 `1회`
4. `npm.cmd test`와 `npm.cmd run build`를 실행해 기준선을 확인한다.
5. 기존 구조를 조사하고, 고정 검사 1만 만족하는 가장 작은 수직 기능을 구현한다.

> 검사 1: 사진이 있으면 `원본과 비교` 버튼으로 비교 모드를 열고 닫을 수 있다.

검사 2·3의 드래그, 비율 계산, 0·50·100 완성은 다음 요청의 몫이다. 이번 요청에서
앞당겨 완성하지 않는다. 다만 검사 1을 지키는 관련 자동 검사는 함께 추가한다.

## 반드시 지킬 것

- `docs/EXPERIMENT_PLAN.md`의 검사 문구·번호·예산·중단 시점을 바꾸지 않는다.
- 비교 UI 상태를 편집 상태, 템플릿 저장 데이터, 공유 링크에 넣지 않는다.
- 기존 카드 렌더러, PNG 출력, 되돌리기·다시하기 동작을 불필요하게 바꾸지 않는다.
- `docs/AI-B-LOG.md`와 `docs/FINAL_COMPARISON.md`를 수정하지 않는다.
- 설치·테스트·빌드·포트·경로 문제는 직접 조사하고, 결과를 기록한다.
- 실행하지 않은 검사를 통과했다고 적지 않는다.
- 사용자 변경과 비밀값을 건드리지 않는다.

## 이번 응답을 끝내기 전에

1. 관련 테스트와 전체 단위 테스트, 빌드를 다시 실행한다.
2. `docs/AI-A-LOG.md` 요청 1행에 누적 분, 한 일, 통과 검사, 남은 오류,
   사람이 바꾼 것을 사실대로 적는다.
3. 이번 구현과 기록만 커밋하고 `origin/main`에 푸시한다.
4. 마지막 응답에는 다음 다섯 가지만 짧게 보고한다.
   - 요청 횟수와 누적 시간
   - 커밋 SHA
   - 실제로 통과한 검사 번호
   - 남은 오류
   - 요청 2에서 가장 먼저 할 일

세 번째 응답 또는 30분 중 먼저 도달한 순간에는 구현을 더 하지 않고
`docs/START-CLAUDE.md`의 계획 중단 절차로 전환한다. 지금 공식 요청 1을 시작하라.
