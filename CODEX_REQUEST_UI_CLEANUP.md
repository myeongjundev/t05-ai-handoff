# 스튜디오 영역 정리 — 코덱스 작업 요청

## 0. 먼저: 이건 실험 밖의 일이다

T05 인계 실험은 `c01fa23` 에서 끝났다. `docs/FINAL_COMPARISON.md` 의 측정표
(단위 85 · e2e 38 · 고정 검사 10)는 **그 커밋의 코드를 잰 값**이다.

그래서 이 작업에는 조건이 하나 붙는다.

- `docs/EXPERIMENT_PLAN.md`, `docs/AI-A-LOG.md`, `docs/AI-B-LOG.md`,
  `docs/FINAL_COMPARISON.md`, `docs/HANDOFF.md` **다섯 개는 이 작업에서 건드리지
  않는다.** 실험 기록은 잰 그대로 남는다.
- 이 작업의 커밋은 전부 `c01fa23` **이후**에 온다. 측정표가 가리키는 코드는
  히스토리에 그대로 있다.
- 이 문서와 작업 기록은 실험 문서가 아니라 제품 작업 문서다.

## 1. 무엇을

지금 공개된 화면(https://myeongjundev.github.io/t05-ai-handoff/)의 스튜디오
영역이 너무 많다. 사용자가 그렇게 느꼈고, 세어 보니 근거가 있었다.

시안은 두 곳에 있다. 링크는 로그인이 필요하니 **저장소 안의 HTML 을 봐라.**

| | |
|---|---|
| `design/00-current-map.html` | 지금 화면의 영역 지도 + 소견 4개 |
| `design/01-option-a-canvas-first.html` | **A · 캔버스 우선 2단 — 이걸 만든다** |
| `design/02-option-b-single-column.html` | B · 한 열 · 한 번에 한 단계 (참고용) |

브라우저로 그냥 열면 된다. `<script src="./support.js">` 는 404 나도 상관없고
`<x-dc>` · `<helmet>` 은 모르는 태그라 무시된다 — 안의 스타일과 마크업은 그대로
그려진다. 색과 치수는 `src/styles.css` 의 실제 토큰에서 가져온 값이다.

같은 것을 볼 수 있는 링크(로그인 필요):
https://claude.ai/code/artifact/6e969464-a34f-4653-a556-139cd3eed96a

**A 안을 구현한다.** B 는 왜 안 골랐는지 보라고 같이 둔 것이다.

## 2. 진단 — 소스에서 센 값

| 항목 | 지금 | 근거 |
|---|---:|---|
| 스튜디오 안의 영역 | 23 | `App.jsx` 의 `.layout` 3열과 그 안의 블록 |
| 회색 설명 문단(`.hint`) | 8 | `EditorPanel.jsx` 6 + `TemplatePanel.jsx` 2 |
| 캔버스 위 조작 줄 | 3 | `PreviewPanel.jsx` `.history-controls` / `.preview-head` 안 2줄 |
| 캔버스 위 버튼 | 8 | 실행취소 · 다시실행 · 편집하며보기 · 작품으로보기 · 원본과비교 · 링크복사 · 이미지복사 · 다운로드 |

읽어야 할 네 가지:

1. **안내가 조작 앞을 막는다.** `.quick-start-guide` 상자는 바로 아래
   ①②③ 단계를 글로 한 번 더 말한다.
2. **카드가 조작 줄에 밀려 내려간다.** 되돌리기 · 보기 방식 · 비율 · 내보내기가
   전부 같은 크기, 같은 회색이라 무엇이 주 행동인지 읽히지 않는다.
3. **세 번째 열이 상시 자리를 차지한다.** 템플릿 저장과 JSON 내보내기 ·
   가져오기는 처음 온 사람이 한 번도 누르지 않는데 화면의 1/4을 쓴다.
   `저장된 템플릿 0개` 가 첫 화면의 고정 문구가 되어 있다.
4. **같은 입력이 두 곳에 있다.** 사진과 문구가 히어로(`.hero-quick`)와
   01 패널 양쪽에 있다.

## 3. 만들 것 — A 안

### 3.1 열을 셋에서 둘로

`styles.css` 의 `.layout` 을 `320px minmax(0,1fr) 340px` 에서
`400px minmax(0,1fr)` 로 바꾼다. 오른쪽은 미리보기가 전부 쓴다.

`TemplatePanel` 은 **지우지 않는다.** 왼쪽 열 맨 아래 접이식 묶음
(`EditorPanel.jsx` 의 `Group` 과 같은 `<details>`)으로 내린다. 제목은
`내 템플릿`, 요약은 `N개 저장됨`. JSON 내보내기 · 가져오기는 그 안에 둔다.

`.panel-templates` 의 sticky 규칙(`styles.css` 981px 미디어쿼리)은 세 번째 열이
없어지면 의미가 없다. 같이 지운다.

### 3.2 캔버스 위 조작 줄 셋을 한 줄로

`PreviewPanel.jsx` 의 `.history-controls` 와 `.preview-head` 를 캔버스 카드에
붙은 **한 줄**로 합친다.

- 왼쪽: 비율 `1:1 4:5 9:16` + 추천 칩 (지금 그대로)
- 오른쪽: 되돌리기/다시하기 아이콘 한 쌍 → 구분선 → `보기 ▾` → `공유 ▾`
  → `이미지 다운로드`(채운 인디고, 유일한 주 행동)

`보기 ▾` 안: 편집하며 보기 · 작품으로 보기 · 원본과 비교 · 안전 영역 가이드.
`공유 ▾` 안: 링크 복사 · 이미지 복사.

되돌리기/다시하기는 아이콘만 남기되 `aria-label` 로 `실행 취소` / `다시 실행` 을
**지금 이름 그대로** 준다. e2e 가 이름으로 찾는다(§5).

`안전 영역 가이드` 토글은 지금 `게시 전 확인` 안에 있는데 `보기 ▾` 로 옮긴다.
9:16 일 때만 나오는 조건은 유지한다.

### 3.3 캔버스를 키운다

`.canvas-frame` 의 `62vh` 를 늘려 카드가 지금(약 460px)보다 크게 나오게 한다.
시안은 560px 기준이다. 캔버스 내부 해상도(1080×H)는 **건드리지 않는다** —
CSS 로만 키운다.

### 3.4 왼쪽 열을 줄인다

- `.quick-start-guide` 상자를 **없앤다.** 단계 번호 칩(`.step-number`)은 남는다.
- 모습 카드 3장을 줄인다. 지금 카드당 5줄(이름 · 한글명 · 시대 · 설명 · 추천
  비율)인데, **이름 + 한 줄 설명**만 남기고 나머지는 `title` 속성으로 옮긴다.
  추천 비율은 이미 미리보기 위 추천 칩이 말하고 있다.
- 설명 문단 8개 → 3개. 남길 셋은 이것이다:
  - 모습 아래 `레이아웃만 바뀝니다. 사진과 문구는 그대로입니다.`
  - 사진 아래 `사진은 이 브라우저 밖으로 나가지 않습니다.`
  - 자동 축소 알림(`.shrink-hint`) — 조건부라 평소엔 안 보인다
- 나머지 다섯은 컨트롤 라벨이나 `title` 로 흡수한다. 지우는 게 아니라 옮긴다.

### 3.5 히어로의 중복 입력

`.hero-quick` 의 사진 · 문구 입력을 **뺀다.** 첫 화면의 주 행동은
`TemporalScanner` 의 `ENTER ERA` 하나로 돌려놓고, 스튜디오로 내려가는 길은
상단 내비의 `제작 도구로 ↘` 가 이미 갖고 있다.

`App.jsx` 안의 관련 주석(왜 여기 입력을 뒀는지 설명하는 긴 주석)은 지우지 말고
**왜 도로 뺐는지로 고쳐 쓴다.** 이 저장소는 판단의 흔적을 남기는 쪽이다.

### 3.6 게시 전 확인

세로 카드에서 **가로 한 줄 띠**로 바꾼다. 왼쪽에 `준비 완료` / `N개 확인`
배지, 오른쪽에 항목 4개를 가로로. 항목 내용과 판정 로직(`readyCheck.js`)은
그대로다.

## 4. 건드리지 말 것

- **카드 렌더러 출력.** `renderCard.js` 가 그리는 픽셀은 이 작업으로 1픽셀도
  달라지면 안 된다. `test/e2e/pixels.e2e.js` 가 잡는다.
- **PNG 내보내기, 되돌리기/다시하기 동작.** 레이아웃 때문에 바꾸지 않는다.
- **비교 모드 상태.** `editorState` · 템플릿 저장 스키마 · 공유 링크 어디에도
  넣지 않는다. 지금처럼 `PreviewPanel` 안의 보기 상태로 둔다.
- **색 토큰과 대비.** `styles.css` 머리말의 실측값이 그대로 유지되어야 한다.
  새로 만드는 팝오버 · 아이콘 버튼도 조작 가능한 경계는 3:1(`--edge`),
  글자는 4.5:1 을 지킨다. `test/e2e/contrast.e2e.js` 가 매번 다시 잰다.
- **§0 의 실험 문서 다섯 개.**
- 기능을 **지우지 않는다.** A 안은 덜어내는 안이 아니라 접는 안이다.
  모든 기능은 클릭 한 번 안에 그대로 있어야 한다.

## 5. e2e 가 깨질 자리 — 미리 말해둔다

지금 e2e 38개는 대부분 역할 · 이름으로 찾으므로 자리를 옮기는 것만으로는
안 깨진다. 다만 **감춰지면 깨진다.** Playwright 는 숨은 요소를 못 누른다.

| 자리 | 참조 수 | 무엇이 문제인가 |
|---|---:|---|
| 보기 방식 버튼 5종 | 14 | `보기 ▾` 안에 들어가면 열기 전에는 못 찾는다 |
| 템플릿(`#template-name` 등) | 5 | `<details>` 가 닫혀 있으면 `fill` 이 실패한다 |
| 모습 카드(`.preset-button` 등) | 14 | 카드에서 줄을 빼면 접근 이름이 달라진다 |

세 가지 다 **테스트를 고쳐서 통과시킨다.** 열어야 하는 곳은 열고, 접근 이름이
바뀌면 새 이름으로 맞춘다. 검사를 지우거나 `skip` 으로 넘기지 않는다.
`.preset-button` 의 `aria-label` 은 지금 형식(`이름, 한글명, 시대: 설명`)을
유지하면 14곳을 안 건드려도 된다 — 화면에서 줄을 빼도 `aria-label` 은 남길 수
있다. 그쪽을 권한다.

## 6. 완료 기준

- `npm test` 85개 통과
- `npm run e2e` 38개 통과 (선택자를 고쳤다면 고친 채로)
- `npm run build` 통과
- 스튜디오 영역 23 → 14 안팎, 설명 문단 8 → 3, 캔버스 위 조작 줄 3 → 1
- 기능 손실 0 — 지금 있는 조작은 전부 클릭 한 번 안에 남아 있을 것
- 1440px 와 390px 두 폭에서 직접 열어 보고 깨진 곳이 없을 것

## 7. 작업 방식

- 판단이 갈리는 자리는 **네 판단으로 하고**, 왜 그렇게 했는지
  `work-log/2026-08-31-ui-cleanup.md` 에 남긴다. 사람에게 되묻느라 멈추지 마라.
- 트러블슈팅(설치 실패, 테스트 · 빌드 오류, 포트 충돌)은 네 몫이다.
- 시안과 다르게 하는 게 낫다고 판단되면 그렇게 하되, 그 이유를 적는다.
  시안은 방향이지 픽셀 명세가 아니다.
- 커밋은 나눠서. `feat:` 하나에 전부 몰아넣지 마라.
