# 최종 비교와 운영 기준

> AI B 완료 뒤 같은 기록 단위로 채우고, 같은 내용을 공개 화면에도 표시한다.
> 아래 최종 문장은 구현 실험 종료 후 제출 정리 단계에서 실행 기록과 다시 대조했다.

| 구분 | AI A | AI B |
|---|---:|---:|
| 작업시간(분) | 11 | 8 |
| 요청(회) | 3 | 1 |
| 배정 검사(개) | 3 | 7 |
| 최종 실행·통과 검사(개/10) | 3 | 10 |
| FAIL 실행 회차(회) | 0 | 0 |
| 남긴 오류(개) | 0 | 0 |
| 재검사한 항목(개) | 1 | 10 |
| 사람이 재작업한 항목(개) | 0 | 0 |
| 인계 이해 오류(개) | 해당 없음 | 1 |

> 이 표는 판정을 위해 모델·서비스 이름을 가린 `AI A` · `AI B` 로만 표기한다.
> 실제 이름은 `docs/AI-A-LOG.md` 와 `docs/AI-B-LOG.md` 에 그대로 공개한다.
> `FAIL 실행 회차` 는 고정 검사 10개를 실행한 회차 중 하나 이상이 FAIL 이 난 회차 수다.

**통과 검사 3 대 7 은 속도 차가 아니다.** 계획이 AI A 에게 요청 1~3 을 주고 그
안에 검사 1~3 만 배정했다. AI A 는 세 번째 응답에서 구현을 멈추고 인계 절차로
전환하도록 지시받았고 그대로 했다. AI B 는 남은 일곱을 받았다. **두 숫자는 각자
받은 몫이지 각자 낸 속도가 아니다.**

같은 이유로 작업시간 11 분과 8 분도 같은 일을 잰 값이 아니다. AI A 의 11 분에는
기준선 확인과 인계 문서 작성이 들어 있고, AI B 의 8 분에는 그것이 없다.

**비교가 성립하는 자리는 따로 있다.** AI B 는 대화 없이 저장소와 `HANDOFF.md`
만으로 남은 일곱을 끝냈고, 인계 문서를 고칠 필요가 없었으며, 사람이 다시 설명해
준 것도 없었다(`사람이 재작업한 항목 0`). 인계 이해 오류 1 건은 구현 오류가
아니라 절차 오류다 — AI B 가 금지 규칙을 읽기 전에 `AI-A-LOG.md` 를 열었다고
스스로 기록했다. AI B 용 시작 지시서가 따로 없어 사람이 세션 첫머리에 금지를
말해 줄 자리가 비어 있었다. **다음 실험에서는 그 문서를 만든다.**

측정 단위: 시간은 사람이 첫 요청을 보낸 시각부터의 벽시계, 요청은 사람이 한 번
보내고 AI 가 한 번 답한 왕복. 도구 호출 수는 세지 않았다.

## 최종 검사

2026-08-31 12:00 KST, AI B 완료 뒤 Codex가 로컬에서 `npm test`(85건) ·
`npm run test:e2e`(38건) · `npm run build`를 실행했다. 이어 같은 커밋의 GitHub
Actions에서도 단위·빌드·실제 Chromium e2e와 Pages 배포가 모두 통과했다.
**아래 `근거` 는 실행한 것만 적는다.**

| # | 결과 | 근거 |
|---:|---|---|
| 1 | 통과 | 단위 `사진이 있으면 열고 닫을 수 있다 — 검사 1`. 화면에서 열기→닫기→다시 열기, 레이어 생성·제거 확인 |
| 2 | 통과 | 단위 `경계선을 옮기면 두 숫자가 함께 바뀐다 — 검사 2`. 화면 30% 에서 clip `inset(0 70% 0 0)` · 경계선 `30%` · `원본 30% · 완성 카드 70%` 가 함께 바뀜 |
| 3 | 통과 | 단위 `0·50·100은 완성만·반반·원본만이 된다 — 검사 3`. 화면 0·50·100 에서 각각 카드 100% · 반반 · 원본 100% |
| 4 | 통과 | e2e `비교 중 시대를 바꾸면 카드만 바뀌고 원본은 유지된다 — 검사 4`. 화면에서 시대 전환 시 캔버스 픽셀 해시는 바뀌고 원본 `src` 는 그대로, 비교 모드 유지 |
| 5 | 통과 | e2e `사진이 없으면 비교할 수 없는 이유가 함께 보인다 — 검사 5`. 사진 없을 때 버튼 `disabled` 이고 `원본과 비교하려면 먼저 사진을 고르세요.` 가 `aria-describedby` 로 연결됨 |
| 6 | 통과 | 단위 2건(`방향키…`, `Home·End…`)과 e2e `방향키·Home·End가 경계선을 0~100 안에서 움직인다 — 검사 6`. 화면에서 Home→0, End→100, 0 에서 ←·100 에서 → 를 눌러도 범위를 벗어나지 않음 |
| 7 | 통과 | e2e `프레임 밖까지 끌면 양 끝에 고정되고 오류가 나지 않는다 — 검사 7`. 화면에서 −50% → 0, 150% → 100, 새 콘솔 오류 없음 |
| 8 | 통과 | e2e `비교 모드를 사용해도 내려받는 PNG 바이트가 같다 — 검사 8`. 화면에서도 비교를 켠 캔버스와 끈 캔버스의 dataURL 이 완전히 같음(541,718자) |
| 9 | 통과 | e2e `비교 모드 뒤에도 편집값 되돌리기와 다시하기가 동작한다 — 검사 9`. 화면에서 되돌리기가 픽셀을 바꾸고 다시하기가 정확히 복원 |
| 10 | 통과 | e2e `템플릿은 편집값만 복원하고 비교 상태를 저장하지 않는다 — 검사 10`. 저장된 템플릿 JSON 과 공유 링크 payload 15개 필드 어디에도 `compare`·`position`·`open` 없음 |

기존 e2e 30건(픽셀 동일성·접근성·스크롤 서사·대비)도 함께 통과했다. 여기에 T05
전용 `compare.e2e.js` 8건을 더해 최종 38건이다. 비교 기능이
캔버스를 다시 그리지 않고 위에 얹기만 하므로 회귀가 생길 자리가 없었다.

## 다음 과제의 선택 기준

- 화면 한 곳에 갇힌 작은 UI는 **Claude의 수직 구현**부터 사용하고, 저장·공유·이력
  경계를 넘는 순간 **Codex의 회귀 검증**으로 바꾼다.
- PNG·템플릿·되돌리기처럼 영향 범위가 넓으면 **Codex의 자동 검사 고정**부터
  사용하고, 화면 언어와 서사가 약할 때 Claude의 검토를 붙인다.
- AI를 바꿀 때는 HANDOFF와 받는 AI의 시작 지시서를 함께 만든다. 사람이 같은 내용을
  다시 설명해야 하면 성공으로 포장하지 않고 인계 실패 또는 문서 보완으로 기록한다.

## AI 3줄

- AI에게 맡긴 일: 기능 구현, 자동 검사, 인계 문서 구조화와 원격 동기화.
- 내가 판단한 일: 비교 기능 선택, Claude→Codex 순서, 계획 중단 시점과 최종 제출 기준.
- AI 말을 안 들은 일: 검사 통과만으로 끝내지 않고, 과정과 판단이 화면에 보이도록
  인계 사례 연구와 30초 검증 동선을 한 단계 더 보강했다.

## 정정 이력

이 문서는 `scripts/verify-handoff.mjs` 가 본문 sha256 으로 봉인하는 보호 문서다.
아래 정정을 반영하면서 봉인 값도 함께 갱신했으므로, 무엇을 왜 고쳤는지 남긴다.

| 일자 | 고친 것 | 이유 |
|---|---|---|
| 2026-09-01 | 비교표 머리글의 모델·서비스 실명을 `AI A` · `AI B` 로 가림 | 제출 기준 T05-C28 이 판정 구간의 비교표에서 이름을 가릴 것을 요구한다. 실명은 두 작업 기록에 그대로 남는다. |
| 2026-09-01 | `통과 검사(개/10) 3 / 7` 을 `배정 검사` 와 `최종 실행·통과 검사(3 / 10)` 두 줄로 나눔 | 같은 문서 아래 `최종 검사` 표가 10개 전부 통과로 적혀 있어 서로 어긋났다. AI B 의 `7` 은 배정 수였고 실제 실행·통과는 10 이다. |
| 2026-09-01 | `FAIL 실행 회차(회)` 줄 추가 | 제출 기준 T05-C25 의 오류 정의(검사 10개 실행 회차 중 FAIL 이 난 회차 수)를 표 안에서 직접 보이게 했다. |

측정값(시간·요청·통과 여부) 자체는 하나도 바꾸지 않았다.
