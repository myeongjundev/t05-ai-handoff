# AI A 작업 기록

| 항목 | 값 |
|---|---|
| AI/모델 | Claude Opus 5 (`claude-opus-5`) — Claude Code 화면 표시값 |
| 작업 환경 | 학원 / 현재 저장소를 연 세션 |
| 시작 시각 | 2026-08-31 11:39 KST |
| 종료 시각 | 2026-08-31 11:50 KST (계획 중단) |
| 누적 시간 | 11분 / 최대 45분 (벽시계) |
| 요청 횟수 | 3회 / 최대 5회 |
| 시작 커밋 | `4dae838787beb2f5ccb57cbbc82de44c5fe442a1` |
| 마지막 커밋 | 요청 3 커밋 (아래 요청 3 행) |

## 이 세션에 대해 먼저 밝힐 것

이 Claude 세션은 공식 요청 1을 받기 전에 같은 사용자의 T04 제출 작업을 했고,
그 과정에서 `docs/START-CLAUDE.md`와 `docs/EXPERIMENT_PLAN.md`의 일부 문구를
사용자 요청으로 다듬었다. 즉 **완전히 깨끗한 세션이 아니다.**

다만 `src/` 이하 카드 스튜디오 구현 코드는 이 요청 전까지 한 번도 열지 않았다.
비교 슬라이더에 대한 사전 지식은 없다. 사실대로 남겨 두어야 AI B와의 비교에서
이 조건을 감안할 수 있다.

| 요청 | 누적 분 | 한 일 | 통과 검사 | 남은 오류 | 사람이 바꾼 것 |
|---:|---:|---|---|---|---|
| 1 | 3분 | 문서 3개·기존 구조 조사, 기준선(테스트 70·빌드) 확인, `src/state/compareMode.js` 순수 모듈과 단위 테스트 6건 추가, `PreviewPanel` 에 `원본과 비교` 토글 연결. dev 서버를 띄워 사진 없음→비활성, 사진 있음→열기·닫기·다시 열기를 실제로 눌러 확인 | 1 | 없음 | 없음 |
| 2 | 10분 | 경계선 위치(0~100)와 클램프를 `compareMode.js` 에 추가하고 단위 테스트 7건 확대. `PreviewPanel` 에 원본 오버레이·경계선·비율 숫자를 붙이고 포인터 드래그 연결. `setPointerCapture` 가 던지면 누른 자리가 유실되는 결함을 찾아 try/catch 로 막음 | 1, 2, 3 | 없음 | 없음 |
| 3 · 계획 중단 | 11분 | 구현 중단. 테스트 83건·빌드 재실행, `docs/HANDOFF.md` 일곱 항목을 실제 값으로 작성, 기록 마무리 | 추가 없음 (1·2·3 유지) | 없음 | 없음 |

## 마지막 집계

- 통과 검사: 3/10 (1·2·3). 단위 테스트와 dev 서버 실제 조작으로 둘 다 확인
- 남긴 오류: 0개
- 재검사한 항목: 1개 — `setPointerCapture` 를 막은 뒤 경계선 이동을 다시 확인
- 사람이 재작업한 항목: 0개. 설치·테스트·빌드·포트 문제는 없었고, 사람에게
  대신 실행해 달라고 요청한 것도 없다
- 계획과 다르게 한 일: 없음. 요청 1 에 검사 1, 요청 2 에 검사 2·3, 요청 3 에
  중단 절차라는 배정을 그대로 지켰다
- 테스트: 70건(시작) → 83건. 새로 쓴 것은 `test/compareMode.test.js` 13건
- 구현 중 찾은 결함 1개: `setPointerCapture` 가 거절되면 누른 자리가 유실됐다.
  포인터 잡기보다 이동을 먼저 하고 `try/catch` 로 감쌌다
- 범위 밖이라 남긴 것 1개: 문구 드래그의 같은 무방비 호출. HANDOFF 5절에 적었다
